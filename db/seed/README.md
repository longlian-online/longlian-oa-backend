# 部署基础数据

`base_data.sql` 是部署基础数据的唯一入口。需要随部署写入数据库的默认配置（例如默认管理端账号、基础字典或系统参数）必须追加到该文件。

每次 `db/migrate.sh <dev|prod> apply` 在结构同步成功后都会执行该文件。SQL 必须可重复执行，且不得覆盖已有业务数据。

`dev_data.sql` 仅用于本地开发环境，不能承载部署基础配置。
